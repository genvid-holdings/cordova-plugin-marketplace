{
  "project": {
    "name": "cordova-plugin-marketplace",
    "description": "Cordova plugin reporting the app's install source / marketplace (Android PackageManager, iOS MarketplaceKit).",
    "languages": ["javascript", "java", "swift"]
  },
  "commands": {
    "test": "npm run test",
    "lint": "npm run lint",
    "typecheck": "npm run typecheck",
    "build": "npm run build",
    "validate": "npm run lint && npm run typecheck && npm run test && npm run build"
  },
  "repo": {
    "host": "github",
    "default_branch": "main"
  },
  "features": {},
  "paths": {}
}
